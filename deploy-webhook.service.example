[Unit]
Description=Game Rocker Deploy Webhook
After=network.target docker.service

[Service]
WorkingDirectory=/opt/game-rocker
EnvironmentFile=/opt/game-rocker/deploy-webhook.env
ExecStart=/opt/game-rocker/deploy-webhook
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
