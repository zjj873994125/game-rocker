# 项目协作规则

## 语言与沟通

- 默认使用中文回复。
- 回复先给结论，再给关键细节。

## 代码与文档

- 新增或修改业务代码时，需要补充必要注释。注释应说明设计意图、非显而易见的约束、资源映射或复杂逻辑，不重复解释语法本身。
- 新增功能、改动核心规则或调整资源映射时，需要同步更新 `docs/` 下的描述文档。
- 如果只是修正错别字、格式化、简单变量重命名，可以不新增文档，但需要保证现有文档不失真。

## 验证

- 修改代码后优先运行最小必要验证，例如 `corepack pnpm run build`。
- 如果没有运行验证，需要在最终回复里说明原因和风险。
